package edu.epam.project.controller.command;

/**
 * The enum Transition type to determine the type of page transition.
 * @author Sargerion.
 */
public enum TransitionType {
    FORWARD,
    REDIRECT
}